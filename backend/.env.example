# Copy to backend/.env and fill in the real value. backend/.env is gitignored.

# Cloudflare Tunnel token, from the Cloudflare Zero Trust dashboard
# (Networks -> Tunnels -> your tunnel -> Docker install command, copy just the --token value)
CLOUDFLARE_TUNNEL_TOKEN=
